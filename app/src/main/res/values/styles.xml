<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!---
      Using Material Design on the App Bar
  -->
    <style name="AppTheme.Base" parent="Theme.AppCompat.Light.NoActionBar">
        <!--Color of Action Bar-->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryDark">@color/primaryColorDark</item>
        <item name="colorAccent">@color/accentColor</item>
    </style>

    <!--
       Custom Toolbar Text and Overflow(triple Dot options)
    -->
    <style name="CustomToolbarTheme" parent="ThemeOverlay.AppCompat.Light">
        <!--Color of Text on the App-->
        <item name="android:textColorPrimary">@color/appBarTextColor</item>
        <!--Color of Triple Dot(more option button-->
        <item name="android:textColorSecondary">@color/appBarTextColor</item>
    </style>

</resources>
